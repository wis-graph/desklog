# 바뀐 것

바꾼 것은 `## 미출시` 절에 그때그때 적는다.
판을 낼 때 `scripts/release.sh` 가 그 절을 판 번호로 확정하고 릴리스 노트로 올린다.
→ [RELEASING.md](RELEASING.md)

## 미출시

- 릴리스 스크립트가 `Cargo.toml` 판 번호를 못 올리던 것을 고쳤다. BSD sed 가 `0,/re/`
  주소 형식을 받지 않아 조용히 실패했고, 그 결과 0.1.2 는 formula 와 태그만 0.1.2 이고
  바이너리는 0.1.1 을 말했다. 편집을 `scripts/bump.py` 로 옮기고, 판 번호가 실제로
  바뀌었는지 스크립트가 스스로 확인하게 했다.

## 0.1.2 (2026-09-06)

- 앱 이름이 `unknown`으로 빠지던 것을 고쳤다. 최전면 앱에 열거 가능한 창이 없으면
  `get_active_window()`가 통째로 실패한다(카카오톡 대화창에서 실제로 그렇다). 이제 실패하면
  `NSWorkspace`에 최전면 앱을 따로 물어본다. 이쪽은 화면 기록 권한이 필요 없다.
- `brew services`로 띄우면 창 제목이 비는 것을 알아냈다. CLI 도구의 화면 기록 권한은
  실행 파일이 아니라 띄운 부모에 붙는데, launchd에는 그 권한이 없다. 앱 이름은 위 대체로
  채워지지만 창 제목은 못 읽는다. → README '알려진 거친 부분'
- `examples/probe.rs` — 창 조회가 무엇을 돌려주는지 그대로 보는 진단용.

## 0.1.1 (2026-09-05)

- `top`에 앱 지정을 더했다. `desklog top 7 카카오톡` 처럼 쓰면 모든 집계를 그 앱으로 좁힌다.
  전에는 1위 앱의 창 제목만 볼 수 있었다.
- README에 sqlite 직접 조회 예제를 넣었다 — 보고만 있던 구간, 늦은 시각 사용, 하루별 총 시간.
- brew 설치·서비스 관리 안내와 알려진 거친 부분을 문서에 적었다.

## 0.1.0 (2026-09-05)

첫 판.

- 활성 앱·창 제목·유휴 시간을 5초마다 관측해서 sqlite에 구간으로 남긴다.
- 구간 저장 — 앱·창 제목·시각(시)·세션 중 하나가 바뀔 때만 새 줄을 연다.
- `watch` `now` `live` `log` `top` `label` `export`
- macOS와 윈도우 한 코드베이스. 윈도우는 아직 실기에서 컴파일하지 않았다.
